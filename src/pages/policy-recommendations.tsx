import Layout from "../components/layout";

const PolicyRecommendations = () => {
  return (
    <Layout>
      <main className="container mx-auto md:flex">Policy Recommendations!</main>
    </Layout>
  );
};

export default PolicyRecommendations;
